language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Home of Travis CI server
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

before_script:
  - chmod +x gradlew

script: "./gradlew clean build"

# alert with email if CI finished
notifications:
  email:
    recipients:
      - wnsghk1025@naver.com